# tinode cluster, behind traefik

# usage

1. fix env
```
cp ./env.example ./env
vim ./env
```

2. excute
```
make up
```

- make down, restart, vim, config, log

# etc
 - default htpasswd ( id: example, pw: example )
